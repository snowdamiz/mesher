---
phase: 01.1-update-project-to-use-mesh-built-in-orm-for-migrations-and-queries
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/user.mpl
  - src/types/project.mpl
  - migrations/20260303100000_create_public_tables.mpl
autonomous: true
requirements: []

must_haves:
  truths:
    - "Model structs exist for every database entity with deriving(Schema, Json, Row)"
    - "A single ORM migration creates all public tables with UUID PKs and gen_random_uuid()"
    - "Projects and api_keys tables are in public schema with org_id FK (no schema-per-org)"
    - "Old TEXT PK migrations are deleted"
  artifacts:
    - path: "src/types/user.mpl"
      provides: "User, Session, OrgMembership, PasswordResetToken, Invite model structs"
      contains: "deriving(Schema, Json, Row)"
    - path: "src/types/project.mpl"
      provides: "Organization, Project, ApiKey model structs"
      contains: "deriving(Schema, Json, Row)"
    - path: "migrations/20260303100000_create_public_tables.mpl"
      provides: "ORM migration with Migration.create_table for all tables"
      contains: "Migration.create_table"
  key_links:
    - from: "src/types/user.mpl"
      to: "src/types/project.mpl"
      via: "OrgMembership belongs_to Organization cross-reference"
      pattern: "belongs_to.*Organization"
    - from: "migrations/20260303100000_create_public_tables.mpl"
      to: "src/types/*.mpl"
      via: "table names must match struct table annotations"
      pattern: "table \"users\""
---

<objective>
Create model struct definitions and fresh ORM migrations for the entire Mesher data layer.

Purpose: Establish the type foundation and database schema that all subsequent ORM query plans depend on. This is the "contracts first" step -- models define the shape of data, migrations create the tables. No hand-written SQL survives in this layer.

Output: Model structs in `src/types/`, a single ORM migration file, and deletion of old raw-SQL migration files.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-update-project-to-use-mesh-built-in-orm-for-migrations-and-queries/01.1-CONTEXT.md

<interfaces>
<!-- Reference project patterns to follow exactly. Source: /Users/sn0w/Documents/dev/mesh/mesher -->

From /Users/sn0w/Documents/dev/mesh/mesher/types/user.mpl (model struct pattern):
```mesh
pub struct User do
  table "users"
  id :: String
  email :: String
  display_name :: String
  created_at :: String
  has_many :org_memberships, OrgMembership
  has_many :sessions, Session
end deriving(Schema, Json, Row)

pub struct Session do
  table "sessions"
  primary_key :token
  token :: String
  user_id :: String
  created_at :: String
  expires_at :: String
  belongs_to :user, User
end deriving(Schema, Json, Row)
```

From /Users/sn0w/Documents/dev/mesh/mesher/types/project.mpl (org/project pattern):
```mesh
pub struct Organization do
  table "organizations"
  id :: String
  name :: String
  slug :: String
  created_at :: String
  has_many :projects, Project
  has_many :org_memberships, OrgMembership
end deriving(Schema, Json, Row)

pub struct ApiKey do
  table "api_keys"
  id :: String
  project_id :: String
  key_value :: String
  label :: String
  created_at :: String
  revoked_at :: Option<String>
  belongs_to :project, Project
end deriving(Schema, Json, Row)
```

From /Users/sn0w/Documents/dev/mesh/mesher/migrations/20260216120000_create_initial_schema.mpl (migration pattern):
```mesh
pub fn up(pool :: PoolHandle) -> Int!String do
  Pool.execute(pool, "CREATE EXTENSION IF NOT EXISTS pgcrypto", [])?

  Migration.create_table(pool, "organizations", [
    "id:UUID:PRIMARY KEY DEFAULT gen_random_uuid()",
    "name:TEXT:NOT NULL",
    "slug:TEXT:NOT NULL UNIQUE",
    "created_at:TIMESTAMPTZ:NOT NULL DEFAULT now()"
  ])?

  Migration.drop_table(pool, "organizations")?
  Ok(0)
end

pub fn down(pool :: PoolHandle) -> Int!String do
  Migration.drop_table(pool, "organizations")?
  Ok(0)
end
```

CRITICAL SCHEMA CHANGES from Phase 1 to ORM version:
- All TEXT PRIMARY KEY columns become UUID PRIMARY KEY DEFAULT gen_random_uuid()
- Drop password_salt column from users (bcrypt handles salting internally via pgcrypto)
- Drop schema_name and owner_id from organizations (add slug instead; no schema-per-org)
- Projects and api_keys move from tenant schemas to public schema with org_id FK
- api_keys: key_hash+key_prefix pattern becomes key_value pattern (matching reference project)
- Sessions: id TEXT PK becomes token TEXT PK (matching reference project)
- Add updated_at to users only where needed
- Invites table stays in public schema with UUID PK
- password_reset_tokens table stays in public schema with UUID PK
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create model struct definitions in src/types/</name>
  <files>src/types/user.mpl, src/types/project.mpl</files>
  <action>
Create two model definition files following the reference project pattern exactly.

**src/types/user.mpl** — Must contain these structs:

1. `User` — table "users", fields: id (String), email (String), password_hash is NOT in struct (never expose to app code, per reference project pattern), created_at (String). Add has_many for org_memberships, sessions. No display_name field (mesher doesn't use it; current schema has no display_name).

2. `OrgMembership` — table "org_memberships", fields: id (String), org_id (String), user_id (String), role (String), created_at (String). Add belongs_to for user and org.

3. `Session` — table "sessions", primary_key :token (not default id), fields: token (String), user_id (String), created_at (String), expires_at (String). Add belongs_to for user.

4. `Invite` — table "invites", fields: id (String), org_id (String), email (String), token (String), invited_by (String), expires_at (String), accepted_at (Option&lt;String&gt;), revoked_at (Option&lt;String&gt;), created_at (String). Add belongs_to for org.

5. `PasswordResetToken` — table "password_reset_tokens", fields: id (String), user_id (String), token_hash (String), expires_at (String), used_at (Option&lt;String&gt;), created_at (String). Add belongs_to for user.

All structs must be `pub struct` with `end deriving(Schema, Json, Row)`.

**src/types/project.mpl** — Must contain these structs:

1. `Organization` — table "organizations", fields: id (String), name (String), slug (String), created_at (String). Add has_many for projects, org_memberships.

2. `Project` — table "projects", fields: id (String), org_id (String), name (String), created_at (String). Add belongs_to for org, has_many for api_keys.

3. `ApiKey` — table "api_keys", fields: id (String), project_id (String), key_hash (String), key_prefix (String), label (String), created_at (String), revoked_at (Option&lt;String&gt;). Add belongs_to for project.

KEY DESIGN DECISIONS:
- ApiKey keeps key_hash + key_prefix pattern (current mesher security model — raw key shown once, stored as SHA-256 hash). Do NOT use key_value pattern from reference project (that stores cleartext keys, which is insecure for a real monitoring platform).
- No password_hash in User struct (per reference project convention).
- Organization has slug (auto-generated from name) instead of schema_name and owner_id. The owner is tracked via OrgMembership with role='owner'.
- Project gains org_id FK (moves from tenant schema to public schema).
- ApiKey gains project_id FK (moves from tenant schema to public schema). Keeps key_hash/key_prefix security model.

Ensure the `src/types/` directory exists before writing files. Create it if needed.
  </action>
  <verify>
    <automated>ls -la src/types/user.mpl src/types/project.mpl && grep -c "deriving(Schema, Json, Row)" src/types/user.mpl src/types/project.mpl</automated>
  </verify>
  <done>Both type files exist with all struct definitions. user.mpl has 5 structs (User, OrgMembership, Session, Invite, PasswordResetToken). project.mpl has 3 structs (Organization, Project, ApiKey). All use deriving(Schema, Json, Row) and table annotations.</done>
</task>

<task type="auto">
  <name>Task 2: Create fresh ORM migration and delete old migrations</name>
  <files>migrations/20260303100000_create_public_tables.mpl, migrations/20260303000000_enable_pgcrypto.mpl, migrations/20260303000001_create_public_tables.mpl</files>
  <action>
**Step 1: Delete old migration files.**
Delete `migrations/20260303000000_enable_pgcrypto.mpl` and `migrations/20260303000001_create_public_tables.mpl`. These contain hand-written raw SQL with TEXT PKs and the old schema.

**Step 2: Create `migrations/20260303100000_create_public_tables.mpl`.**

This single migration creates ALL tables in the public schema. Follow the reference project migration pattern exactly (`pub fn up(pool :: PoolHandle) -> Int!String`).

The migration must:

1. Enable pgcrypto: `Pool.execute(pool, "CREATE EXTENSION IF NOT EXISTS pgcrypto", [])?`

2. Create tables in FK dependency order using `Migration.create_table`:

   a. `organizations` — id:UUID:PRIMARY KEY DEFAULT gen_random_uuid(), name:TEXT:NOT NULL, slug:TEXT:NOT NULL UNIQUE, created_at:TIMESTAMPTZ:NOT NULL DEFAULT now()

   b. `users` — id:UUID:PRIMARY KEY DEFAULT gen_random_uuid(), email:TEXT:NOT NULL UNIQUE, password_hash:TEXT:NOT NULL, created_at:TIMESTAMPTZ:NOT NULL DEFAULT now()

   c. `org_memberships` — Use raw Pool.execute (Migration.create_table cannot express composite UNIQUE or FK references cleanly). id:UUID PK DEFAULT gen_random_uuid(), user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE, org_id UUID NOT NULL REFERENCES organizations(id) ON DELETE CASCADE, role TEXT NOT NULL DEFAULT 'member', created_at TIMESTAMPTZ NOT NULL DEFAULT now(), UNIQUE(user_id, org_id)

   d. `sessions` — token:TEXT:PRIMARY KEY, user_id:UUID:NOT NULL REFERENCES users(id) ON DELETE CASCADE, created_at:TIMESTAMPTZ:NOT NULL DEFAULT now(), expires_at:TIMESTAMPTZ:NOT NULL DEFAULT now() + interval '7 days'

   e. `invites` — id:UUID:PRIMARY KEY DEFAULT gen_random_uuid(), org_id:UUID:NOT NULL REFERENCES organizations(id) ON DELETE CASCADE, email:TEXT:NOT NULL, token:TEXT:NOT NULL UNIQUE, invited_by:UUID:NOT NULL REFERENCES users(id), expires_at:TIMESTAMPTZ:NOT NULL, accepted_at:TIMESTAMPTZ, revoked_at:TIMESTAMPTZ, created_at:TIMESTAMPTZ:NOT NULL DEFAULT now()

   f. `password_reset_tokens` — id:UUID:PRIMARY KEY DEFAULT gen_random_uuid(), user_id:UUID:NOT NULL REFERENCES users(id) ON DELETE CASCADE, token_hash:TEXT:NOT NULL UNIQUE, expires_at:TIMESTAMPTZ:NOT NULL, used_at:TIMESTAMPTZ, created_at:TIMESTAMPTZ:NOT NULL DEFAULT now()

   g. `projects` — id:UUID:PRIMARY KEY DEFAULT gen_random_uuid(), org_id:UUID:NOT NULL REFERENCES organizations(id) ON DELETE CASCADE, name:TEXT:NOT NULL, created_at:TIMESTAMPTZ:NOT NULL DEFAULT now()

   h. `api_keys` — id:UUID:PRIMARY KEY DEFAULT gen_random_uuid(), project_id:UUID:NOT NULL REFERENCES projects(id) ON DELETE CASCADE, key_hash:TEXT:NOT NULL, key_prefix:TEXT:NOT NULL, label:TEXT:NOT NULL DEFAULT 'default', created_at:TIMESTAMPTZ:NOT NULL DEFAULT now(), revoked_at:TIMESTAMPTZ

3. Create indexes using `Pool.execute`:
   - idx_org_memberships_user ON org_memberships(user_id)
   - idx_org_memberships_org ON org_memberships(org_id)
   - idx_sessions_user ON sessions(user_id)
   - idx_sessions_expires ON sessions(expires_at)
   - idx_invites_token ON invites(token)
   - idx_invites_email ON invites(email)
   - idx_projects_org ON projects(org_id)
   - idx_api_keys_project ON api_keys(project_id)
   - idx_api_keys_hash ON api_keys(key_hash) WHERE revoked_at IS NULL

4. `pub fn down(pool :: PoolHandle) -> Int!String` must drop tables in reverse FK order using `Migration.drop_table`.

Use `?` operator throughout for error propagation. End with `Ok(0)`.

NOTE: For tables with FK REFERENCES and composite UNIQUE constraints (org_memberships, invites, password_reset_tokens, projects, api_keys), use raw `Pool.execute` with full CREATE TABLE SQL since Migration.create_table may not support FK syntax in the constraint string. Check the reference project — it uses Migration.create_table for sessions with "user_id:UUID:NOT NULL REFERENCES users(id) ON DELETE CASCADE" inline, so the Migration DSL DOES support FK references in the constraint portion. Use Migration.create_table where possible; fall back to Pool.execute only for composite UNIQUE constraints that need a separate clause.
  </action>
  <verify>
    <automated>ls migrations/ && grep -c "Migration.create_table" migrations/20260303100000_create_public_tables.mpl && test ! -f migrations/20260303000000_enable_pgcrypto.mpl && test ! -f migrations/20260303000001_create_public_tables.mpl && echo "OK"</automated>
  </verify>
  <done>Old migrations deleted. New single migration file exists with Migration.create_table calls for all tables, UUID PKs with gen_random_uuid(), proper FK references, indexes, and up/down functions. No password_salt column. No schema_name/owner_id on organizations. Projects and api_keys in public schema with org_id FK.</done>
</task>

</tasks>

<verification>
1. `src/types/user.mpl` contains 5 pub structs with deriving(Schema, Json, Row)
2. `src/types/project.mpl` contains 3 pub structs with deriving(Schema, Json, Row)
3. All structs have `table "tablename"` annotations matching migration table names
4. Migration file uses Migration.create_table DSL (not raw DDL) for most tables
5. All PKs are UUID with gen_random_uuid() (except sessions which uses token TEXT PK)
6. No password_salt column anywhere
7. No schema_name or owner_id on organizations
8. Old migration files are deleted
</verification>

<success_criteria>
- 8 model structs defined across 2 type files
- 1 migration file creates all 8 tables with proper FK relationships
- Old migrations deleted
- Compilation check: structs use valid Mesh syntax (pub struct ... do ... end deriving(...))
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-update-project-to-use-mesh-built-in-orm-for-migrations-and-queries/01.1-01-SUMMARY.md`
</output>
