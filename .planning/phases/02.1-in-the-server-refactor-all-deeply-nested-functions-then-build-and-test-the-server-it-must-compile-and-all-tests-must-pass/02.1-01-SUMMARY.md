---
phase: 02.1-in-the-server-refactor-all-deeply-nested-functions-then-build-and-test-the-server-it-must-compile-and-all-tests-must-pass
plan: 01
subsystem: ingest
tags: [mesh, ingest, refactor, otlp, sentry]
requires:
  - phase: 02-error-ingestion-core
    provides: ingest handlers, auth/rate-limit pipeline, event persistence
provides:
  - Shared ingest helper module for duplicate json/brace/bracket logic
  - Shared ingest preflight middleware for auth, rate limit, and body validation
  - Flattened generic and envelope handlers with shallower control flow
  - Unified OTLP logs/traces path via one parameterized internal handler
  - Flattened scrubber key-value replacement branch logic
affects: [phase-02.1-plan-02, phase-02.1-plan-03, ingest, server-refactor]
tech-stack:
  added: []
  patterns: [thin-handler preflight wrapper, result-helper extraction, module-level parity gating]
key-files:
  created:
    - server/src/ingest/helpers.mpl
    - server/src/ingest/middleware.mpl
  modified:
    - server/src/ingest/generic.mpl
    - server/src/ingest/envelope.mpl
    - server/src/ingest/otlp.mpl
    - server/src/ingest/scrubber.mpl
key-decisions:
  - "Kept protocol-specific response semantics while centralizing preflight in validate_ingest_request."
  - "Unified OTLP logs and traces through handle_otlp_mode(mode) while preserving public handlers."
  - "Applied strict module-by-module green gates with one checkpoint commit per ingest module."
patterns-established:
  - "Ingest handlers call validate_ingest_request once, then delegate to focused parsing/persistence helpers."
  - "Duplicate string JSON helpers belong in Src.Ingest.Helpers."
requirements-completed: [N/A]
duration: 21 min
completed: 2026-03-04
---

# Phase 02.1 Plan 01: Ingest Flattening Summary

**Ingest handler control flow was flattened with shared preflight and helper modules while preserving API signatures and response semantics.**

## Performance

- **Duration:** 21 min
- **Started:** 2026-03-04T05:53:28Z
- **Completed:** 2026-03-04T06:15:12Z
- **Tasks:** 3
- **Files modified:** 6

## Accomplishments

- Added `helpers.mpl` for exact duplicate `json_field`, `find_matching_brace`, and `find_matching_bracket`.
- Added `middleware.mpl` with `validate_ingest_request` to centralize auth, rate limiting, and body guards.
- Flattened `handle_generic_event` into thin preflight + result helper flow.
- Flattened envelope processing with pair-progress helpers and a focused payload processor.
- Unified OTLP logs/traces through one parameterized internal mode handler while preserving exported route handlers.
- Flattened `scrub_json_key_value` branching into focused helper functions without behavior changes.

## Task Commits

Each module gate was committed atomically in required order:

1. **Task 1: Create shared ingest helper and middleware modules, then wire imports**
   - `a404b52` refactor(02.1-01): flatten ingest helpers
   - `0e43149` refactor(02.1-01): flatten ingest middleware
2. **Task 2: Flatten critical ingest functions and unify OTLP log/trace handling**
   - `df66298` refactor(02.1-01): flatten ingest generic
   - `172e648` refactor(02.1-01): flatten ingest envelope
   - `c22a01a` refactor(02.1-01): flatten ingest otlp
   - `8e23e74` refactor(02.1-01): flatten ingest scrubber
3. **Task 3: Enforce module-by-module ingest parity gates**
   - Verified via strict ordered build/test gates before each module commit above.

## Files Created/Modified

- `server/src/ingest/helpers.mpl` - Shared duplicate string/json/brace/bracket helpers.
- `server/src/ingest/middleware.mpl` - Shared ingest preflight wrapper with protocol-aware error responses.
- `server/src/ingest/generic.mpl` - Flattened generic handler and extracted result-based processing helpers.
- `server/src/ingest/envelope.mpl` - Flattened envelope item iteration and payload processing flow.
- `server/src/ingest/otlp.mpl` - Unified logs/traces mode handling and flattened attribute extraction.
- `server/src/ingest/scrubber.mpl` - Flattened `scrub_json_key_value` logic with helper extraction.

## Decisions Made

- Preserved all public route handler signatures (`handle_generic_event`, `handle_sentry_envelope`, `handle_otlp_logs`, `handle_otlp_traces`) unchanged.
- Kept OTLP/internal processing failure behavior parity (`partialSuccess` responses remain 200 when payload sections are absent or internal item processing fails).
- Retained Sentry-specific 429 header behavior via middleware protocol mode.

## Deviations from Plan

None - plan executed exactly as written.

## Authentication Gates

None.

## Issues Encountered

- Concurrent non-phase edits appeared in `server/src/auth/*`; these were explicitly ignored per user direction and not staged in 02.1-01 commits.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Ingest refactor baseline is green and checkpoint-committed module-by-module.
- Ready for `02.1-02-PLAN.md`.

---
*Phase: 02.1-in-the-server-refactor-all-deeply-nested-functions-then-build-and-test-the-server-it-must-compile-and-all-tests-must-pass*
*Completed: 2026-03-04*

## Self-Check: PASSED

- Verified SUMMARY file exists.
- Verified all module checkpoint commits exist in git history.
