# Phase 1.1: Update project to use Mesh built-in ORM for migrations and queries - Context

**Gathered:** 2026-03-03
**Status:** Ready for planning

<domain>
## Phase Boundary

Replace all hand-written raw SQL (migrations, queries, transactions) with Mesh's built-in ORM across the entire codebase. This is a refactoring phase — no new features, no new endpoints, no schema changes. The application behavior is identical before and after; only the data access layer changes.

</domain>

<decisions>
## Implementation Decisions

### Conversion scope
- Full conversion — no raw SQL strings left anywhere in the codebase
- Convert module by module (not all at once), verifying each module before moving to the next
- Conversion order: Claude's discretion based on ORM capabilities discovered during research
- While converting queries, also clean up deeply nested case/match error handling (use ? operator where possible, flatten nesting)

### Migration files
- Fresh start — delete old migration files, create new ORM migrations from scratch
- Dev databases can be wiped (`docker compose down -v && docker compose up`)
- Study the reference project at `/Users/sn0w/Documents/dev/mesh/mesher` to understand what the ORM migration runner supports and how migrations work
- If Mesh ORM has a built-in migration runner, use it instead of the current manual `up(pool)/down(pool)` pattern

### Tenant isolation
- ORM for everything — even schema creation and tenant table provisioning should use the ORM if possible
- How `with_org_schema` (SET LOCAL search_path) integrates with the ORM: Claude decides after researching the reference project
- Schema naming convention (currently `org_{uuid}`): Claude decides if a better pattern exists

### Model definitions
- File location: Claude decides based on ORM conventions and reference project patterns
- Validation rules (email format, name length): Claude decides whether to move into models or keep in handlers
- Relationships (has_many, belongs_to): Claude decides based on ORM support and current query needs
- ID strategy (currently TEXT UUIDs via Crypto.uuid4()): Claude decides based on ORM defaults

### Claude's Discretion
- Conversion order across modules (migrations, auth, org, project, db)
- Model file organization (dedicated directory vs co-located with handlers)
- Validation placement (models vs handlers)
- Relationship definitions (if ORM supports them)
- ID generation strategy
- Schema naming convention
- Unified vs separate API for public/tenant queries
- `with_org_schema` replacement approach

</decisions>

<specifics>
## Specific Ideas

- "You will need to double check this and study the dogfooded project `/Users/sn0w/Documents/dev/mesh/mesher` to understand what can be done and what can't" — the reference Mesh project is the source of truth for ORM capabilities
- The user wants the ORM used everywhere possible, falling back to raw SQL only where the ORM literally cannot do something
- Error handling cleanup is explicitly in scope — flatten the deeply nested case blocks while converting

</specifics>

<code_context>
## Existing Code Insights

### Reusable Assets
- `src/db/tenant.mpl` (`with_org_schema`): Core tenant isolation helper — needs ORM conversion or replacement
- `src/auth/cookies.mpl` (`extract_user_id`): Auth helper used by all handlers — no SQL, stays as-is

### Established Patterns
- Two query patterns: `Pool.query`/`Pool.execute` for public schema, `Pg.query`/`Pg.execute` inside `with_org_schema` for tenant schema
- Manual transaction management: `Pool.checkout` → `Pg.begin` → operations → `Pg.commit`/`Pg.rollback` → `Pool.checkin`
- Migration format: `fn up(pool)` / `fn down(pool)` with raw DDL strings
- Module imports: `from Src.Module import func` with `pub fn` for exported functions

### Integration Points
- `main.mpl`: Entry point — creates pool via `Pool.open`, passes to all handlers. ORM may change pool initialization
- `migrations/`: 2 files with `up/down` pattern — will be deleted and replaced
- `src/org/schema.mpl`: `provision_org_schema` — creates tenant schemas with raw DDL, needs ORM conversion
- All 7 handler files import and use raw SQL — every handler file will be touched

### Files to Convert (all .mpl files with raw SQL)
1. `migrations/20260303000000_enable_pgcrypto.mpl` — DELETE
2. `migrations/20260303000001_create_public_tables.mpl` — DELETE
3. `src/db/tenant.mpl` — `with_org_schema` helper
4. `src/org/schema.mpl` — `provision_org_schema`
5. `src/auth/session.mpl` — login, logout, session CRUD, auth middleware
6. `src/auth/reset.mpl` — password reset tokens
7. `src/auth/oauth.mpl` — Google OAuth user creation
8. `src/org/handlers.mpl` — org CRUD (deepest nesting — most cleanup)
9. `src/org/invites.mpl` — invite CRUD
10. `src/project/projects.mpl` — project and API key CRUD
11. `main.mpl` — pool initialization may change

</code_context>

<deferred>
## Deferred Ideas

None — discussion stayed within phase scope.

</deferred>

---

*Phase: 01.1-update-project-to-use-mesh-built-in-orm-for-migrations-and-queries*
*Context gathered: 2026-03-03*
