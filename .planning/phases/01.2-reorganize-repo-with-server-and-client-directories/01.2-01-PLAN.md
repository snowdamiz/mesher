---
phase: 01.2-reorganize-repo-with-server-and-client-directories
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/main.mpl
  - server/src/Config.mpl
  - server/migrations/20260303100000_create_public_tables.mpl
  - server/mesh.toml
  - server/mesh.lock
  - client/package.json
  - client/src/lib/api.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Backend code is rooted under server/ with unchanged Mesh module/import behavior."
    - "Frontend app is renamed to client/ and continues using relative /api calls."
  artifacts:
    - path: "server/main.mpl"
      provides: "Backend entrypoint relocated under server/."
      contains: "from Src."
    - path: "server/src/"
      provides: "All backend modules moved under server/src."
      contains: "auth, org, project, storage, types, db"
    - path: "client/src/lib/api.ts"
      provides: "Client API adapter after frontend->client rename."
      contains: "/api"
  key_links:
    - from: "server/main.mpl"
      to: "server/src/*"
      via: "Mesh imports"
      pattern: "from Src\\."
    - from: "client/src/lib/api.ts"
      to: "/api"
      via: "relative fetch targets"
      pattern: "['\\\"]\\/api"
---

<objective>
Relocate code into explicit `server/` and `client/` roots with no runtime behavior changes.

Purpose: Enforce clear ownership boundaries before any new feature work, while honoring locked naming and path decisions.
Output: Moved backend project root and renamed client app directory.
</objective>

<execution_context>
@/Users/sn0w/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.2-reorganize-repo-with-server-and-client-directories/01.2-CONTEXT.md
@.planning/phases/01.2-reorganize-repo-with-server-and-client-directories/01.2-RESEARCH.md
@main.mpl
@src/Config.mpl
@frontend/src/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move backend project root under server/</name>
  <files>server/main.mpl, server/src/, server/migrations/, server/mesh.toml, server/mesh.lock</files>
  <action>
Create `server/` and move backend project assets using history-preserving moves (`git mv`), including `main.mpl`, `src/`, `migrations/`, `mesh.toml`, and `mesh.lock`. Keep module names and imports unchanged (no namespace redesign in this phase). Do not move `.planning/` or `.gitnexus/` from repository root.
  </action>
  <verify>
    <automated>test -f server/main.mpl && test -d server/src && test -d server/migrations && test -f server/mesh.toml && test -f server/mesh.lock && test ! -f main.mpl && test ! -d src && test ! -d migrations</automated>
  </verify>
  <done>Backend runtime files are fully rooted under `server/` and the old root backend paths are removed.</done>
</task>

<task type="auto">
  <name>Task 2: Rename frontend app directory to client/ without API contract drift</name>
  <files>client/, client/src/lib/api.ts</files>
  <action>
Rename `frontend/` to `client/` with `git mv`. Keep source layout as `client/src` and preserve existing API request semantics in `client/src/lib/api.ts` with relative `/api` paths only (no absolute host rewrite). Do not introduce framework/toolchain changes.
  </action>
  <verify>
    <automated>test -d client/src && test -f client/package.json && test ! -d frontend && rg -n "['\\\"]\\/api" client/src/lib/api.ts</automated>
  </verify>
  <done>`client/` replaces `frontend/`, and API calls remain relative and behavior-compatible.</done>
</task>

</tasks>

<verification>
1. Backend files compile-path location changed from root to `server/` only.
2. Frontend directory is renamed to `client/` and still references relative `/api`.
</verification>

<success_criteria>
- `server/main.mpl`, `server/src`, `server/migrations`, `server/mesh.toml`, and `server/mesh.lock` exist.
- `client/` exists and `frontend/` does not.
</success_criteria>

<output>
After completion, create `.planning/phases/01.2-reorganize-repo-with-server-and-client-directories/01.2-01-SUMMARY.md`
</output>
