---
status: passed
phase: 01.2-reorganize-repo-with-server-and-client-directories
verified_on: 2026-03-03
verifier: codex
---

# Phase 01.2 Verification

## Inputs Reviewed
- `.planning/phases/01.2-reorganize-repo-with-server-and-client-directories/01.2-01-PLAN.md`
- `.planning/phases/01.2-reorganize-repo-with-server-and-client-directories/01.2-02-PLAN.md`
- `.planning/phases/01.2-reorganize-repo-with-server-and-client-directories/01.2-03-PLAN.md`
- `.planning/phases/01.2-reorganize-repo-with-server-and-client-directories/01.2-01-SUMMARY.md`
- `.planning/phases/01.2-reorganize-repo-with-server-and-client-directories/01.2-02-SUMMARY.md`
- `.planning/phases/01.2-reorganize-repo-with-server-and-client-directories/01.2-03-SUMMARY.md`

## Phase Goal
Reorganize the repository into explicit `server/` and `client/` roots while preserving current runtime behavior, path semantics, and developer workflow.

## Must-Have Validation

### 1) Directory layout and ownership boundaries
- `server/` and `client/` exist at repo root; `frontend/` is absent.
- Backend runtime assets are rooted under `server/` (`main.mpl`, `src/`, `migrations/`, `mesh.toml`, `mesh.lock`).
- Server-only `.mpl` spikes are under `server/spikes/`; browser `.html` spikes remain under root `spikes/`.

Result: **Pass**

### 2) Compose wiring and service contract
- `docker-compose.yml` defines service `server` with `build.context: ./server` and `dockerfile: Dockerfile`.
- `docker compose config --services` resolves: `server`, `timescaledb`, `valkey` (no `app` service).

Result: **Pass**

### 3) Root workflow scripts and scoped ownership
- Root `package.json` exposes required wrappers:
  - `dev:server`, `build:server`, `test:server`, `migrate:status`, `migrate:up`
  - `dev:client`, `build:client`, `test:client`
- Root package remains dependency-light (no runtime dependency hoisting).
- `.gitignore` uses `client/node_modules` and `client/dist` paths.

Result: **Pass**

### 4) Documentation alignment
- `README.md`, `CLAUDE.md`, and `AGENTS.md` consistently describe `server/` + `client/` layout, `server` compose service naming, and root wrapper commands.
- No active-doc drift found for legacy `frontend/` or `app` service terminology.

Result: **Pass**

### 5) `/api` path semantics
- `client/src/lib/api.ts` uses relative `/api` request targets and no absolute host binding.

Result: **Pass**

## Regression Command Evidence
- `npm --prefix client run build` passed.
- `npm run build:client` passed.
- `meshc build server` and `npm run build:server` passed in a clean detached worktree at current `HEAD`.

Note: `meshc build server` in the current working tree reproduced the previously documented local hang behavior, but clean-worktree verification confirms phase output itself is valid.

## Gaps Found
None.

## Final Verdict
**passed** — Phase 01.2 goal achievement is verified against codebase reality with no remaining must-have gaps.
