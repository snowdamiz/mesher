---
phase: 01.2-reorganize-repo-with-server-and-client-directories
plan: 03
subsystem: infra
tags: [documentation, regression, compose, mesh, vite]
requires:
  - phase: 01.2-02
    provides: compose wiring, root wrappers, and server/client ownership boundaries
provides:
  - Active contributor docs standardized to server/client terminology
  - Reorg regression matrix verified across server build, client build, root wrappers, compose services, and /api path semantics
affects: [phase-completion, onboarding, local-dev-workflow]
tech-stack:
  added: []
  patterns:
    - Root docs mirror executable wrapper commands and directory ownership
    - Validation matrix recorded with clean-worktree fallback when dirty workspace is not representative
key-files:
  created:
    - README.md
  modified:
    - AGENTS.md
    - CLAUDE.md
    - package.json
    - docker-compose.yml
    - client/src/lib/api.ts
key-decisions:
  - "Keep active docs authoritative on server/client naming and root wrapper commands."
  - "Treat local dirty-workspace meshc hang as unrelated to phase output; validate server build in detached clean worktree at HEAD."
patterns-established:
  - "Use clean detached worktree verification when unrelated local edits invalidate compile checks."
requirements-completed: []
duration: 18min
completed: 2026-03-04
---

# Phase 01.2 Plan 03: Docs + Regression Matrix Summary

**Active docs now consistently use `server/` + `client/`, and the reorganization verification matrix is satisfied with explicit evidence.**

## Performance

- **Duration:** 18 min
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- Added `README.md` with current repo structure and root workflow (`dev:server`, `dev:client`, `build:server`, `build:client`).
- Updated `CLAUDE.md` and `AGENTS.md` to align instructions with `server/` + `client/` and `server` compose service terminology.
- Verified client build paths and compose/service/API wiring remain consistent after the structural reorg.

## Task Commits

Each task was committed atomically:

1. **Task 1: Standardize active docs on server/client terminology and commands** - `c0c1874` (chore)
2. **Task 2: Run regression matrix and patch any missed path drift** - `da9b97f` (test)

## Files Created/Modified
- `README.md` - Canonical structure/command reference for contributors.
- `CLAUDE.md` - Updated local automation guidance for server/client layout.
- `AGENTS.md` - Updated local agent instructions for server/client layout.

## Verification Evidence
- `npm --prefix client run build` ✅
- `npm run build:client` ✅
- `docker compose config --services` returns only `server`, `timescaledb`, `valkey` ✅
- `client/src/lib/api.ts` uses relative `/api` endpoints ✅
- `meshc build server` and `npm run build:server` ✅ in detached clean worktree at `HEAD`

## Deviations from Plan

### Auto-fixed / Documented

**1. [Rule 3 - Blocking] `meshc build server` hangs in dirty workspace with unrelated local edits**
- **Found during:** Task 2 verification
- **Issue:** Server build command did not complete in current dirty workspace.
- **Fix:** Verified server build and root wrapper server build in detached clean worktree at current `HEAD`, isolating phase outputs from unrelated local edits.
- **Impact:** No reorg path drift found; matrix remained green against committed phase output.

---

**Total deviations:** 1 documented blocking-environment deviation (resolved via clean-worktree validation)

## Issues Encountered
None tied to phase output.

## User Setup Required
None.

## Next Phase Readiness
- Phase 01.2 is ready for verification and completion routing.
- Structural `server/` + `client/` split is documented and validated.

## Self-Check: PASSED
