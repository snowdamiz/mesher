---
phase: 02.1-in-the-server-refactor-all-deeply-nested-functions-then-build-and-test-the-server-it-must-compile-and-all-tests-must-pass
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/auth/session.mpl
  - server/src/auth/reset.mpl
  - server/src/auth/oauth.mpl
  - server/src/auth/cookies.mpl
autonomous: true
requirements: ["N/A"]

must_haves:
  truths:
    - "All targeted auth functions with 3+ nesting levels are flattened while preserving responses and side effects"
    - "`session.mpl` login/logout/middleware behavior remains unchanged (status codes, cookie handling, and auth checks)"
    - "`reset.mpl` preserves anti-enumeration behavior and token/session invalidation flow after flattening"
    - "`oauth.mpl` preserves SaaS gate and guard-error mapping while flattening callback/start control flow"
    - "Server still compiles and tests pass after auth refactors"
  artifacts:
    - path: "server/src/auth/session.mpl"
      provides: "Flattened login/logout/middleware control flow"
      contains: "handle_login"
    - path: "server/src/auth/reset.mpl"
      provides: "Flattened request/confirm reset handlers"
      contains: "confirm_reset_handler"
    - path: "server/src/auth/oauth.mpl"
      provides: "Flattened OAuth start/callback branching"
      contains: "google_oauth_callback"
    - path: "server/src/auth/cookies.mpl"
      provides: "Shared auth cookie extraction helpers used by refactored auth paths when applicable"
      contains: "extract_user_id"
  key_links:
    - from: "server/src/auth/session.mpl"
      to: "server/src/storage/queries.mpl"
      via: "authenticate_user/create_session/delete_session/validate_session"
      pattern: "from Src.Storage.Queries import"
    - from: "server/src/auth/reset.mpl"
      to: "server/src/mail/sender.mpl"
      via: "password reset email dispatch"
      pattern: "send_email"
    - from: "server/src/auth/oauth.mpl"
      to: "server/src/auth/guards.mpl"
      via: "query param validation and guard_error mapping"
      pattern: "require_query|guard_error"
    - from: "server/src/auth/session.mpl"
      to: "server/src/auth/cookies.mpl"
      via: "shared cookie parsing/extraction patterns"
      pattern: "mesher_session"
---

<objective>
Refactor nested auth handlers and middleware into flatter, easier-to-reason control flow without changing behavior.

Purpose: These auth modules contain multiple 3-level branch chains that are hard to maintain. This plan applies the user’s lighter-touch auth strategy while keeping all existing auth contracts intact.

Output: Flattened auth modules (`session`, `reset`, `oauth`, optional cookie-helper consolidation), with parity-verified compile and tests.
</objective>

<execution_context>
@/Users/sn0w/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-in-the-server-refactor-all-deeply-nested-functions-then-build-and-test-the-server-it-must-compile-and-all-tests-must-pass/02.1-CONTEXT.md
@.planning/phases/02.1-in-the-server-refactor-all-deeply-nested-functions-then-build-and-test-the-server-it-must-compile-and-all-tests-must-pass/02.1-RESEARCH.md

@server/src/auth/session.mpl
@server/src/auth/reset.mpl
@server/src/auth/oauth.mpl
@server/src/auth/cookies.mpl
@server/src/auth/guards.mpl
@server/src/storage/queries.mpl

<interfaces>
From server/src/auth/session.mpl:
```mesh
pub fn handle_login(pool, request) -> Response
pub fn handle_logout(pool, request) -> Response
```

From server/src/auth/reset.mpl:
```mesh
pub fn request_reset_handler(pool, request) -> Response
pub fn confirm_reset_handler(pool, request) -> Response
```

From server/src/auth/oauth.mpl:
```mesh
pub fn google_oauth_start(pool, request) -> Response
pub fn google_oauth_callback(pool, request) -> Response
```

From server/src/auth/cookies.mpl:
```mesh
pub fn extract_user_id(pool, request) -> String!String
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flatten session auth handlers and middleware (lighter-touch)</name>
  <files>server/src/auth/session.mpl</files>
  <action>
Before changing auth files, run GitNexus preflight checks with explicit repo targeting:
1. `gitnexus_query({repo: "mesher", query: "phase 02.1-02 auth session reset oauth cookies flatten refactor", goal: "pre-edit process visibility"})`
2. `gitnexus_context({repo: "mesher", name: "handle_login", file_path: "server/src/auth/session.mpl"})`
3. `gitnexus_context({repo: "mesher", name: "confirm_reset_handler", file_path: "server/src/auth/reset.mpl"})`
4. `gitnexus_context({repo: "mesher", name: "google_oauth_callback", file_path: "server/src/auth/oauth.mpl"})`
5. `gitnexus_impact({repo: "mesher", target: "session.mpl", direction: "upstream"})`
6. `gitnexus_impact({repo: "mesher", target: "oauth.mpl", direction: "upstream"})`

Refactor `handle_login`, `handle_logout`, and `auth_middleware` to reduce nested case/if depth while preserving:
- response status codes and bodies
- session cookie semantics (`Set-Cookie`, clear cookie behavior)
- cleanup/session validation side effects

Apply helper extraction only where it clearly improves readability; do not force architecture churn for already-simple 3-level paths.

Keep public handler signatures unchanged and maintain existing imports from storage queries.
  </action>
  <verify>
    <automated>npm run build:server && npm run test:server</automated>
  </verify>
  <done>Session module reads as flat control flow, and login/logout/middleware behavior is unchanged from pre-refactor semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Flatten reset + OAuth handlers and consolidate shared auth helper patterns</name>
  <files>server/src/auth/reset.mpl, server/src/auth/oauth.mpl, server/src/auth/cookies.mpl</files>
  <action>
Refactor nested branches in:
- `reset.mpl`: `request_reset_handler`, `confirm_reset_handler`
- `oauth.mpl`: `google_oauth_start`, `google_oauth_callback`

Preserve locked behavior:
- reset request always returns success response on unknown email (anti-enumeration)
- confirm reset preserves token validation + session invalidation flow
- OAuth remains SaaS-only and keeps guard error mapping

Consolidate duplicated cookie/session helper patterns into `cookies.mpl` only where it reduces duplication without changing module boundaries.
  </action>
  <verify>
    <automated>npm run build:server && npm run test:server</automated>
  </verify>
  <done>Targeted auth functions are flattened, behavior parity is preserved, and any helper consolidation in cookies.mpl remains backwards-compatible.</done>
</task>

<task type="auto">
  <name>Task 3: Run auth module-by-module parity gates with checkpoint commits</name>
  <files>server/src/auth/session.mpl, server/src/auth/reset.mpl, server/src/auth/oauth.mpl, server/src/auth/cookies.mpl</files>
  <action>
Apply the locked build/test strategy for auth:
1. Process auth modules in strict order: `session` -> `reset` -> `oauth` -> `cookies` (if changed).
2. After finishing each module, run `npm run build:server` then `npm run test:server`.
3. If both commands pass, create a checkpoint commit before the next module:
   `git add <module-file> && git commit -m "refactor(02.1-02): flatten auth <module>"`
4. Resolve failures immediately; do not continue with a red state.
5. Keep this phase pure refactor: no new auth endpoints, no schema changes, no behavior changes.
6. Record parity outcomes and checkpoint commit hashes for each module in the summary.
  </action>
  <verify>
    <automated>npm run build:server && npm run test:server && git log --oneline -n 4</automated>
  </verify>
  <done>All auth refactors are completed with green build+test gates, per-module checkpoint commits, and no behavior regressions introduced.</done>
</task>

</tasks>

<verification>
- All listed auth target functions are flatter and readable
- Anti-enumeration, SaaS-only, and guard mapping behaviors are preserved
- Public auth handlers remain signature-compatible with current route wiring
- GitNexus preflight `query/context/impact` checks are run with `repo: "mesher"` before edits
- Each auth module has a post-green checkpoint commit
- `npm run build:server` passes
- `npm run test:server` passes
</verification>

<success_criteria>
- `handle_login`, `handle_logout`, `auth_middleware`, `request_reset_handler`, `confirm_reset_handler`, `google_oauth_start`, and `google_oauth_callback` are all refactored with reduced branch depth
- Any cookie/helper extraction remains centralized and reusable without behavior drift
- Every auth module change is checkpoint-committed after a green build+test gate
- Server compile and test suite remain green after auth refactor completion
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-in-the-server-refactor-all-deeply-nested-functions-then-build-and-test-the-server-it-must-compile-and-all-tests-must-pass/02.1-02-SUMMARY.md`
</output>
