---
phase: 02.1-in-the-server-refactor-all-deeply-nested-functions-then-build-and-test-the-server-it-must-compile-and-all-tests-must-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/ingest/helpers.mpl
  - server/src/ingest/middleware.mpl
  - server/src/ingest/generic.mpl
  - server/src/ingest/envelope.mpl
  - server/src/ingest/otlp.mpl
  - server/src/ingest/scrubber.mpl
autonomous: true
requirements: ["N/A"]

must_haves:
  truths:
    - "All critical ingest handlers are flattened: `handle_generic_event`, `handle_sentry_envelope`, `handle_otlp_logs`, and `handle_otlp_traces` no longer rely on deep inline case/if pyramids"
    - "Shared ingest duplicate helpers are centralized in `server/src/ingest/helpers.mpl` and consumed by generic, envelope, and otlp modules"
    - "A shared preflight wrapper (`validate_ingest_request`) in `server/src/ingest/middleware.mpl` performs API-key auth, rate-limit check, and body extraction once per handler"
    - "OTLP logs and traces handlers are unified through one parameterized ingest path while keeping public handler signatures and route behavior unchanged"
    - "Server still compiles and tests pass after ingest refactors"
  artifacts:
    - path: "server/src/ingest/helpers.mpl"
      provides: "Exact-duplicate helper consolidation for json field and bracket/brace matching"
      exports: ["json_field", "find_matching_brace", "find_matching_bracket"]
    - path: "server/src/ingest/middleware.mpl"
      provides: "Shared ingest preflight wrapper for auth + rate limit + body extraction"
      exports: ["validate_ingest_request"]
    - path: "server/src/ingest/generic.mpl"
      provides: "Flattened generic JSON handler using shared middleware/helpers"
      contains: "handle_generic_event"
    - path: "server/src/ingest/envelope.mpl"
      provides: "Flattened envelope handler and simplified item processing flow"
      contains: "handle_sentry_envelope"
    - path: "server/src/ingest/otlp.mpl"
      provides: "Unified OTLP log/trace processing path with preserved response contract"
      contains: "handle_otlp_logs"
    - path: "server/src/ingest/scrubber.mpl"
      provides: "Flattened scrubber branch logic with behavior parity"
      contains: "scrub_json_key_value"
  key_links:
    - from: "server/src/ingest/middleware.mpl"
      to: "server/src/ingest/auth.mpl"
      via: "extract_and_validate_api_key preflight"
      pattern: "extract_and_validate_api_key"
    - from: "server/src/ingest/middleware.mpl"
      to: "server/src/ingest/ratelimit.mpl"
      via: "check_rate_limit preflight"
      pattern: "check_rate_limit"
    - from: "server/src/ingest/generic.mpl"
      to: "server/src/ingest/middleware.mpl"
      via: "single preflight call before payload-specific logic"
      pattern: "validate_ingest_request"
    - from: "server/src/ingest/envelope.mpl"
      to: "server/src/ingest/helpers.mpl"
      via: "shared json/brace/bracket helper imports"
      pattern: "from Src.Ingest.Helpers import"
    - from: "server/src/ingest/otlp.mpl"
      to: "server/src/ingest/helpers.mpl"
      via: "shared json field helper imports"
      pattern: "from Src.Ingest.Helpers import"
---

<objective>
Refactor the ingest domain to remove deep nesting, deduplicate exact helper logic, and centralize handler preflight behavior.

Purpose: This is the highest-risk area of the phase and must complete all structural flattening before the slot-pipe pass. The output must preserve all endpoint semantics while making the control flow shallow and maintainable.

Output: New `helpers.mpl` and `middleware.mpl` modules, flattened ingest handlers (`generic`, `envelope`, `otlp`), and flattened scrubber branching.
</objective>

<execution_context>
@/Users/sn0w/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-in-the-server-refactor-all-deeply-nested-functions-then-build-and-test-the-server-it-must-compile-and-all-tests-must-pass/02.1-CONTEXT.md
@.planning/phases/02.1-in-the-server-refactor-all-deeply-nested-functions-then-build-and-test-the-server-it-must-compile-and-all-tests-must-pass/02.1-RESEARCH.md

@server/main.mpl
@server/src/ingest/auth.mpl
@server/src/ingest/ratelimit.mpl
@server/src/ingest/generic.mpl
@server/src/ingest/envelope.mpl
@server/src/ingest/otlp.mpl
@server/src/ingest/scrubber.mpl
@server/src/storage/queries.mpl

<interfaces>
From server/main.mpl:
```mesh
from Src.Ingest.Envelope import handle_sentry_envelope
from Src.Ingest.Otlp import handle_otlp_logs, handle_otlp_traces, handle_otlp_metrics
from Src.Ingest.Generic import handle_generic_event
```

From server/src/ingest/auth.mpl:
```mesh
pub fn extract_and_validate_api_key(pool :: PoolHandle, request) -> Map<String, String>!String
```

From server/src/ingest/ratelimit.mpl:
```mesh
pub fn check_rate_limit(pool :: PoolHandle, org_id :: String, limit :: Int) -> Map<String, String>
pub fn compute_retry_after() -> String
```

From server/src/storage/queries.mpl:
```mesh
pub fn upsert_issue(pool, project_id, fingerprint, title, level) -> Map<String, String>!String
pub fn insert_event(pool, project_id, issue_id, event_id, timestamp, platform, level, message, exception_type, exception_value, stacktrace_json, environment, release_tag, server_name, tags_json, extra_json, contexts_json, sdk_name, sdk_version, fingerprint) -> String!String
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared ingest helper and middleware modules, then wire imports</name>
  <files>server/src/ingest/helpers.mpl, server/src/ingest/middleware.mpl, server/src/ingest/generic.mpl, server/src/ingest/envelope.mpl, server/src/ingest/otlp.mpl</files>
  <action>
Before changing ingest files, run GitNexus preflight checks with explicit repo targeting:
1. `gitnexus_query({repo: "mesher", query: "phase 02.1-01 ingest helpers middleware generic envelope otlp refactor", goal: "pre-edit process visibility"})`
2. `gitnexus_context({repo: "mesher", name: "handle_generic_event", file_path: "server/src/ingest/generic.mpl"})`
3. `gitnexus_context({repo: "mesher", name: "handle_sentry_envelope", file_path: "server/src/ingest/envelope.mpl"})`
4. `gitnexus_context({repo: "mesher", name: "handle_otlp_logs", file_path: "server/src/ingest/otlp.mpl"})`
5. `gitnexus_impact({repo: "mesher", target: "generic.mpl", direction: "upstream"})`
6. `gitnexus_impact({repo: "mesher", target: "otlp.mpl", direction: "upstream"})`

Implement `server/src/ingest/helpers.mpl` and move only exact duplicates into it:
- `json_field` (currently duplicated in generic/otlp with equivalent behavior)
- `find_matching_brace` (+ required depth helper)
- `find_matching_bracket` (+ required depth helper)

Implement `server/src/ingest/middleware.mpl` with a shared `validate_ingest_request()` wrapper that performs, in order:
1. API key extraction/validation
2. Rate limit check (including Retry-After value extraction)
3. Request body extraction/basic empty-body guard

The wrapper must return validated request context (`project_id`, `org_id`, `body`) or a ready-to-return error `Response`, so each handler has one top-level branch boundary.

Update imports in generic/envelope/otlp to consume shared helper/middleware modules while preserving public handler signatures and route compatibility.

Keep Mesh ordering constraints: leaf helpers first, public fns last.
  </action>
  <verify>
    <automated>npm run build:server && npm run test:server</automated>
  </verify>
  <done>`helpers.mpl` and `middleware.mpl` exist, compile, and are used by ingest handlers; no public route signatures changed.</done>
</task>

<task type="auto">
  <name>Task 2: Flatten critical ingest functions and unify OTLP log/trace handling</name>
  <files>server/src/ingest/generic.mpl, server/src/ingest/envelope.mpl, server/src/ingest/otlp.mpl, server/src/ingest/scrubber.mpl</files>
  <action>
Refactor all ingest targets from CONTEXT.md with behavior parity:
- `generic.mpl:handle_generic_event`
- `envelope.mpl:handle_sentry_envelope`, `process_items`
- `otlp.mpl:handle_otlp_logs`, `handle_otlp_traces`, `get_otlp_attr`, and discretionary flattening of `process_log_records` / `process_trace_exceptions`
- `scrubber.mpl:scrub_json_key_value`

Apply the locked ingest pattern: extract `-> Type!String` helpers where needed, use `?` inside helpers, and keep handlers as thin Response mappers.

Unify `handle_otlp_logs` and `handle_otlp_traces` behind one parameterized internal function while keeping both public handlers exported and behavior-compatible.

For recursive functions, flatten only when readability improves without changing termination conditions or event processing semantics.
  </action>
  <verify>
    <automated>npm run build:server && npm run test:server</automated>
  </verify>
  <done>All targeted ingest functions are flattened, OTLP logs/traces share one parameterized path, and endpoint behavior/status codes remain unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce module-by-module ingest parity gates</name>
  <files>server/src/ingest/helpers.mpl, server/src/ingest/middleware.mpl, server/src/ingest/generic.mpl, server/src/ingest/envelope.mpl, server/src/ingest/otlp.mpl, server/src/ingest/scrubber.mpl</files>
  <action>
Apply the locked build/test strategy while finishing ingest refactors:
1. Process modules in strict order: `helpers` -> `middleware` -> `generic` -> `envelope` -> `otlp` -> `scrubber`.
2. After completing each module, run `npm run build:server` then `npm run test:server`.
3. If both commands pass, create a checkpoint commit before moving to the next module:
   `git add <module-file> && git commit -m "refactor(02.1-01): flatten ingest <module>"`
4. Fix all failures before moving to the next module (no red-state carryover).
5. Keep changes behavior-preserving; do not add new endpoints or features.
6. Record module completion, gate results, and checkpoint commit hashes in the plan summary.
  </action>
  <verify>
    <automated>npm run build:server && npm run test:server && git log --oneline -n 6</automated>
  </verify>
  <done>Each ingest module reached a green build+test gate and has a checkpoint commit before proceeding; final ingest refactor state is fully green.</done>
</task>

</tasks>

<verification>
- Shared helper and middleware modules compile and are imported by ingest handlers
- Critical ingest target functions are flattened and readable (no deep branch pyramids)
- OTLP logs/traces public handlers are preserved but share a parameterized core path
- GitNexus preflight `query/context/impact` checks are run with `repo: "mesher"` before edits
- Each ingest module has a post-green checkpoint commit
- `npm run build:server` passes
- `npm run test:server` passes
</verification>

<success_criteria>
- `server/src/ingest/helpers.mpl` exists and owns the duplicated helper logic listed in CONTEXT.md
- `server/src/ingest/middleware.mpl` exists and centralizes ingest preflight
- `handle_generic_event`, `handle_sentry_envelope`, `handle_otlp_logs`, and `handle_otlp_traces` are flattened and compile
- `scrub_json_key_value` is flattened with behavior parity
- Every ingest module change is checkpoint-committed after a green build+test gate
- Server compiles and tests pass after ingest refactor completion
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-in-the-server-refactor-all-deeply-nested-functions-then-build-and-test-the-server-it-must-compile-and-all-tests-must-pass/02.1-01-SUMMARY.md`
</output>
