---
phase: 02.1-in-the-server-refactor-all-deeply-nested-functions-then-build-and-test-the-server-it-must-compile-and-all-tests-must-pass
plan: 03
type: execute
wave: 2
depends_on: ["02.1-01", "02.1-02"]
files_modified:
  - server/src/ingest/generic.mpl
  - server/src/ingest/envelope.mpl
  - server/src/ingest/otlp.mpl
  - server/src/ingest/scrubber.mpl
  - server/src/auth/session.mpl
  - server/src/auth/reset.mpl
  - server/src/auth/oauth.mpl
autonomous: true
requirements: ["N/A"]

must_haves:
  truths:
    - "Pipe operators are applied as a separate second pass after flattening (user-locked sequencing)"
    - "Both standard and slot pipe forms are used where they improve clarity (`|>`, `|2>`, `|3>`, `|4>`) and `|1>` is never introduced"
    - "Slot-pipe placements preserve behavior for functions where `pool` is arg1 and data flows into later parameters"
    - "Server compiles and all automated server tests pass after the pipe pass"
    - "If full ingestion integration script cannot run due missing runtime services, this is explicitly documented with reason"
  artifacts:
    - path: "server/src/ingest/generic.mpl"
      provides: "Pipe-based value flow in generic ingestion path"
      contains: "|>"
    - path: "server/src/ingest/envelope.mpl"
      provides: "Pipe-based value flow in envelope parsing/processing path"
      contains: "|>"
    - path: "server/src/ingest/otlp.mpl"
      provides: "Pipe/slot-pipe value flow in OTLP processing path"
      contains: "|2>"
    - path: "server/src/auth/session.mpl"
      provides: "Pipe-based value flow for auth handler/middleware branches"
      contains: "|>"
    - path: "server/src/auth/reset.mpl"
      provides: "Pipe-based value flow for reset handlers"
      contains: "|>"
  key_links:
    - from: "server/src/ingest/otlp.mpl"
      to: "server/src/storage/queries.mpl"
      via: "slot-pipe into functions where data is not arg1"
      pattern: "\\|[2-9]>"
    - from: "server/src/ingest/generic.mpl"
      to: "server/src/ingest/middleware.mpl"
      via: "preflight output piped into ingest-specific processing"
      pattern: "validate_ingest_request"
    - from: "server/src/auth/session.mpl"
      to: "server/src/storage/queries.mpl"
      via: "auth/session query chain preserved through pipe rewrites"
      pattern: "authenticate_user|create_session|validate_session"
---

<objective>
Run the dedicated second pass for aggressive pipe-operator adoption, then complete final compile/test gates for the server refactor phase.

Purpose: The user explicitly wants real-world dogfooding of Mesh slot pipes. This pass must be isolated from structural refactors to keep regressions easy to localize.

Output: Ingest and auth modules updated with `|>`/`|N>` where appropriate, with final server compile and test suite green.
</objective>

<execution_context>
@/Users/sn0w/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-in-the-server-refactor-all-deeply-nested-functions-then-build-and-test-the-server-it-must-compile-and-all-tests-must-pass/02.1-CONTEXT.md
@.planning/phases/02.1-in-the-server-refactor-all-deeply-nested-functions-then-build-and-test-the-server-it-must-compile-and-all-tests-must-pass/02.1-RESEARCH.md

# Dependency outputs (created during execution of depends_on plans)
@.planning/phases/02.1-in-the-server-refactor-all-deeply-nested-functions-then-build-and-test-the-server-it-must-compile-and-all-tests-must-pass/02.1-01-SUMMARY.md
@.planning/phases/02.1-in-the-server-refactor-all-deeply-nested-functions-then-build-and-test-the-server-it-must-compile-and-all-tests-must-pass/02.1-02-SUMMARY.md

@server/src/ingest/generic.mpl
@server/src/ingest/envelope.mpl
@server/src/ingest/otlp.mpl
@server/src/ingest/scrubber.mpl
@server/src/auth/session.mpl
@server/src/auth/reset.mpl
@server/src/auth/oauth.mpl
@server/tests/test_ingestion.sh

<interfaces>
Pipe semantics to enforce from locked decision:
```text
x |> f(a, b)    => f(x, a, b)
x |2> f(a, b)   => f(a, x, b)
x |3> f(a, b)   => f(a, b, x)
|1> is invalid (use |> instead)
```

Keep these public signatures unchanged:
```mesh
pub fn handle_generic_event(pool :: PoolHandle, request) -> Response
pub fn handle_sentry_envelope(pool :: PoolHandle, request) -> Response
pub fn handle_otlp_logs(pool :: PoolHandle, request) -> Response
pub fn handle_otlp_traces(pool :: PoolHandle, request) -> Response
pub fn handle_login(pool, request) -> Response
pub fn handle_logout(pool, request) -> Response
pub fn request_reset_handler(pool, request) -> Response
pub fn confirm_reset_handler(pool, request) -> Response
pub fn google_oauth_start(pool, request) -> Response
pub fn google_oauth_callback(pool, request) -> Response
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run GitNexus pre-refactor safety checkpoint, then execute ingest module-by-module pipe pass</name>
  <files>server/src/ingest/generic.mpl, server/src/ingest/envelope.mpl, server/src/ingest/otlp.mpl, server/src/ingest/scrubber.mpl</files>
  <action>
Before changing code, run required GitNexus safety mapping with explicit repo selection:
1. `gitnexus_query({repo: "mesher", query: "phase 02.1 plan 03 ingest/auth pipe pass modules", goal: "pre-refactor process visibility"})`
2. `gitnexus_context({repo: "mesher", name: "generic.mpl", file_path: "server/src/ingest/generic.mpl"})`
3. `gitnexus_context({repo: "mesher", name: "otlp.mpl", file_path: "server/src/ingest/otlp.mpl"})`
4. `gitnexus_impact({repo: "mesher", target: "generic.mpl", direction: "upstream"})`
5. `gitnexus_impact({repo: "mesher", target: "otlp.mpl", direction: "upstream"})`

Then execute ingest pipe refactor in strict module order with per-module gates and checkpoint commits:
1. `server/src/ingest/generic.mpl`
2. `server/src/ingest/envelope.mpl`
3. `server/src/ingest/otlp.mpl`
4. `server/src/ingest/scrubber.mpl`

For each module in order:
- Apply aggressive `|>` and slot-pipe (`|2>`, `|3>`, `|4>`) rewrites where value flow is clearer.
- Never introduce `|1>`.
- Preserve behavior/status/signatures.
- Run `npm run build:server && npm run test:server` immediately.
- Create a checkpoint commit before moving on: `git add <module-file> && git commit -m "refactor(02.1-03): pipe-pass <module>"`.
- If build/tests fail, fix before continuing (no red-state carryover).
  </action>
  <verify>
    <automated>npm run build:server && npm run test:server && git log --oneline -n 4</automated>
  </verify>
  <done>GitNexus pre-refactor safety checkpoint is recorded and each ingest module has an isolated green build+test gate plus checkpoint commit.</done>
</task>

<task type="auto">
  <name>Task 2: Execute auth module-by-module pipe pass with parity gates and checkpoint commits</name>
  <files>server/src/auth/session.mpl, server/src/auth/reset.mpl, server/src/auth/oauth.mpl</files>
  <action>
Apply the same dedicated pipe pass in strict auth module order:
1. `server/src/auth/session.mpl`
2. `server/src/auth/reset.mpl`
3. `server/src/auth/oauth.mpl`

For each module in order:
- Replace sequential value-flow let chains with `|>` where transformed value is argument 1.
- Use slot pipes only when argument positions require it and readability remains clear.
- Preserve anti-enumeration, SaaS-only gating, guard-error mapping, and session/cookie behavior exactly.
- Run `npm run build:server && npm run test:server` after that module change.
- Create checkpoint commit before next module: `git add <module-file> && git commit -m "refactor(02.1-03): pipe-pass <module>"`.
- Do not proceed until the module gate is green.
  </action>
  <verify>
    <automated>npm run build:server && npm run test:server && git log --oneline -n 3</automated>
  </verify>
  <done>Session/reset/oauth modules each pass a local build+test gate and have checkpoint commits with preserved behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Run GitNexus post-refactor scope checkpoint and final regression gates</name>
  <files>server/src/ingest/generic.mpl, server/src/ingest/envelope.mpl, server/src/ingest/otlp.mpl, server/src/ingest/scrubber.mpl, server/src/auth/session.mpl, server/src/auth/reset.mpl, server/src/auth/oauth.mpl</files>
  <action>
Run GitNexus post-refactor scope check before finalizing:
1. `gitnexus_detect_changes({repo: "mesher", scope: "all"})`
2. Confirm changed files/processes align with this plan (`generic`, `envelope`, `otlp`, `scrubber`, `session`, `reset`, `oauth` plus planning artifacts).
3. If unexpected files/processes appear, resolve before final phase completion.

Then run final mandatory gates:
1. `npm run build:server`
2. `npm run test:server`
3. Optional integration gate if services are already running and seeded: `bash server/tests/test_ingestion.sh`

If integration gate is skipped, document exactly why (for example: server/DB services unavailable) in the summary; compile + test commands remain mandatory. Include GitNexus detect-changes output snapshot in the summary.
  </action>
  <verify>
    <automated>npm run build:server && npm run test:server</automated>
  </verify>
  <done>GitNexus post-refactor checkpoint confirms expected scope and mandatory compile/tests are green; optional ingestion integration gate is pass or explicitly environment-blocked.</done>
</task>

</tasks>

<verification>
- Pipe pass is executed only after wave-1 flattening plans complete
- GitNexus pre-refactor `query/context/impact` checkpoint is completed with `repo: "mesher"`
- Each module change is gated by immediate `npm run build:server && npm run test:server` before continuing
- Each module has its own checkpoint commit after green gates
- `|>` and `|N>` are applied aggressively where fit, without semantic changes
- GitNexus post-refactor `detect_changes` confirms expected blast radius
- `npm run build:server` passes
- `npm run test:server` passes
- Optional `server/tests/test_ingestion.sh` outcome is captured (pass or blocked reason)
</verification>

<success_criteria>
- Dedicated Wave 2 pipe pass completed across ingest + auth refactored modules
- Module order (`generic` → `envelope` → `otlp` → `scrubber` → `session` → `reset` → `oauth`) is enforced with per-module green gates and commits
- GitNexus safety checkpoints are documented before (`query/context/impact`) and after (`detect_changes`) refactor execution
- No public server handler signatures changed
- Server compiles successfully
- All server automated tests pass at phase end
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-in-the-server-refactor-all-deeply-nested-functions-then-build-and-test-the-server-it-must-compile-and-all-tests-must-pass/02.1-03-SUMMARY.md`
</output>
