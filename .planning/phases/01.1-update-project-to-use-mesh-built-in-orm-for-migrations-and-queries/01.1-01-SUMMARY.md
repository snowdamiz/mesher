---
phase: 01.1-update-project-to-use-mesh-built-in-orm-for-migrations-and-queries
plan: 01
subsystem: database
tags: [mesh-orm, postgres, migrations, models, uuid, schema]

# Dependency graph
requires:
  - phase: 01-foundation
    provides: "Original raw-SQL migrations and table structure"
provides:
  - "8 ORM model structs (User, OrgMembership, Session, Invite, PasswordResetToken, Organization, Project, ApiKey)"
  - "Single ORM migration creating all public tables with UUID PKs"
  - "Type definitions for all database entities with deriving(Schema, Json, Row)"
affects: [01.1-02, 01.1-03, 01.1-04]

# Tech tracking
tech-stack:
  added: [mesh-orm-migration, mesh-schema-derive]
  patterns: [model-structs-with-deriving, migration-create-table-dsl, uuid-pk-gen-random-uuid]

key-files:
  created:
    - src/types/user.mpl
    - src/types/project.mpl
    - migrations/20260303100000_create_public_tables.mpl
  modified: []

key-decisions:
  - "ApiKey keeps key_hash/key_prefix security model instead of reference project cleartext key_value"
  - "password_hash excluded from User struct (never expose to app code)"
  - "Organization uses slug instead of schema_name/owner_id (no schema-per-org)"
  - "org_memberships uses raw Pool.execute for composite UNIQUE constraint; all other tables use Migration.create_table DSL"

patterns-established:
  - "Model struct pattern: pub struct Name do table \"name\" ... end deriving(Schema, Json, Row)"
  - "Type file organization: user-related in types/user.mpl, org/project-related in types/project.mpl"
  - "Migration pattern: pub fn up/down with PoolHandle, ? operator throughout, Ok(0) return"

requirements-completed: []

# Metrics
duration: 2min
completed: 2026-03-03
---

# Phase 01.1 Plan 01: Model Structs and ORM Migration Summary

**8 ORM model structs in src/types/ with deriving(Schema, Json, Row) and single migration creating all public tables with UUID PKs via gen_random_uuid()**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-03T19:15:43Z
- **Completed:** 2026-03-03T19:17:16Z
- **Tasks:** 2
- **Files modified:** 5 (2 created, 2 deleted, 1 created)

## Accomplishments
- Created 8 model struct definitions across 2 type files with full ORM annotations
- Single migration file creates all 8 tables with UUID PKs, FK references, and 9 performance indexes
- Deleted 2 old raw-SQL migration files (TEXT PKs, password_salt, schema_name/owner_id)
- Established type foundation that all subsequent ORM query plans depend on

## Task Commits

Each task was committed atomically:

1. **Task 1: Create model struct definitions in src/types/** - `0796a5c` (feat)
2. **Task 2: Create fresh ORM migration and delete old migrations** - `4ef37ca` (feat)

## Files Created/Modified
- `src/types/user.mpl` - User, OrgMembership, Session, Invite, PasswordResetToken model structs
- `src/types/project.mpl` - Organization, Project, ApiKey model structs
- `migrations/20260303100000_create_public_tables.mpl` - ORM migration for all 8 tables
- `migrations/20260303000000_enable_pgcrypto.mpl` - DELETED (replaced by consolidated migration)
- `migrations/20260303000001_create_public_tables.mpl` - DELETED (replaced by ORM migration)

## Decisions Made
- ApiKey retains key_hash/key_prefix security model (SHA-256 hash storage) rather than reference project's cleartext key_value pattern -- appropriate for a real monitoring platform
- password_hash intentionally excluded from User struct per reference project convention (never expose to app code; queried directly in auth SQL)
- Organization uses slug (auto-generated from name) instead of schema_name and owner_id -- schema-per-org eliminated in favor of org_id FK filtering
- org_memberships table created via raw Pool.execute due to composite UNIQUE(user_id, org_id) constraint; all other 7 tables use Migration.create_table DSL

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Type definitions ready for import by query conversion plans (01.1-02, 01.1-03, 01.1-04)
- Migration ready to run against fresh database (`docker compose down -v && docker compose up`)
- All struct table annotations match migration table names exactly

## Self-Check: PASSED

- FOUND: src/types/user.mpl
- FOUND: src/types/project.mpl
- FOUND: migrations/20260303100000_create_public_tables.mpl
- FOUND: commit 0796a5c (Task 1)
- FOUND: commit 4ef37ca (Task 2)

---
*Phase: 01.1-update-project-to-use-mesh-built-in-orm-for-migrations-and-queries*
*Completed: 2026-03-03*
