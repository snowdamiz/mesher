---
phase: 02.1-in-the-server-refactor-all-deeply-nested-functions-then-build-and-test-the-server-it-must-compile-and-all-tests-must-pass
status: human_needed
verified_on: 2026-03-04
verifier: codex
---

# Phase 02.1 Verification

## Verdict
Core phase goal is implemented in the codebase and mandatory compile/test gates are green, but full behavior-parity confidence still needs a human/runtime check.

## Evidence
- Ingest dedup + shared preflight exist and are wired:
  - `server/src/ingest/helpers.mpl:14`, `:30`, `:46` export shared `find_matching_brace`, `find_matching_bracket`, `json_field`.
  - `server/src/ingest/middleware.mpl:36` defines `validate_ingest_request` with auth -> rate-limit -> body checks.
  - Each ingest handler calls preflight once:
    - `server/src/ingest/generic.mpl:164`
    - `server/src/ingest/envelope.mpl:355`
    - `server/src/ingest/otlp.mpl:321`
- Critical handler flattening/unification is present:
  - `handle_generic_event`: `server/src/ingest/generic.mpl:163`
  - `handle_sentry_envelope`: `server/src/ingest/envelope.mpl:354`
  - OTLP logs/traces unified through internal mode handler:
    - `handle_otlp_mode`: `server/src/ingest/otlp.mpl:316`
    - wrappers: `handle_otlp_logs` `:340`, `handle_otlp_traces` `:350`
- Auth refactor targets are present with flatter helper-driven flow:
  - `handle_login`/`handle_logout`/`auth_middleware`: `server/src/auth/session.mpl:131`, `:143`, `:149`
  - `request_reset_handler`/`confirm_reset_handler`: `server/src/auth/reset.mpl:102`, `:115`
  - `google_oauth_start`/`google_oauth_callback`: `server/src/auth/oauth.mpl:58`, `:95`
- Pipe pass evidence:
  - `|>` and slot pipes (`|2>`, `|3>`, `|4>`) are present across target ingest/auth modules.
  - No `|1>` usage found under `server/src`.
- Recent commit history aligns with phase scope:
  - Ingest flatten: `a404b52`, `0e43149`, `df66298`, `172e648`, `c22a01a`, `8e23e74`
  - Auth flatten: `8b06bb1`, `f47a94b`
  - Pipe pass: `d11d5cc`, `16707b6`, `4394f71`, `a6667c7`, `1371d47`, `7b64fc1`, `9c00cf4`
  - Docs/summaries: `6734876`, `520027d`, `66589d6`
- Mandatory gates re-verified now:
  - `npm run build:server` passed (`meshc build server` compiled).
  - `npm run test:server` passed (all test groups passed).

## Must-Haves Validation (Plans 01/02/03)
- Plan 01 must-haves: met in current code (shared ingest helpers/middleware, flattened handlers, unified OTLP path, green build/test).
- Plan 02 must-haves: met in current code (flattened auth targets, preserved response/status/cookie patterns, green build/test).
- Plan 03 must-haves: met in current code (`|>`, `|2>`, `|3>`, `|4>` used; no `|1>`; green build/test).

## Requirement ID Cross-Reference
- `02.1-01-PLAN.md`, `02.1-02-PLAN.md`, and `02.1-03-PLAN.md` frontmatter each declare `requirements: ["N/A"]`.
- No requirement IDs from `.planning/REQUIREMENTS.md` are referenced by 02.1 plan frontmatter, so there are no requirement-ID mappings to reconcile for this phase.

## Gaps / Human Follow-Up
1. Runtime integration parity is not fully machine-verified in this environment:
- `bash server/tests/test_ingestion.sh` currently fails with `Server not running at http://localhost:8080`.
- This matches documented blocking context in `02.1-03-SUMMARY.md` and is acceptable for plan documentation, but it leaves final behavior-parity confidence dependent on a live-stack run.

2. Evidence granularity gap for 02.1-02 parity sequencing:
- `f47a94b` combines reset+oauth(+cookies) changes rather than one checkpoint per module.
- `d686149` records parity execution but contains no file diff/log artifact.
- This does not contradict end-state code correctness, but strict per-module gate traceability is incomplete from git artifacts alone.
