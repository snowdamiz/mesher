---
phase: 01.2-reorganize-repo-with-server-and-client-directories
plan: 02
subsystem: infra
tags: [docker, compose, meshc, repo-structure, scripts]
requires:
  - phase: 01.2-01
    provides: server and client directory split completed
provides:
  - Docker Compose server service wired to build from server/
  - Root-scoped server/client command wrappers with app-scoped dependency ownership
  - Server-only Mesh spikes moved under server/spikes/
affects: [phase-01.2-03, docs, local-dev-workflow]
tech-stack:
  added: []
  patterns:
    - Root package.json as wrapper-only orchestration entrypoint
    - Compose service naming aligned to directory ownership (server)
key-files:
  created:
    - package.json
    - server/spikes/mesh_language_issues_v3.test.mpl
  modified:
    - docker-compose.yml
    - server/Dockerfile
    - .gitignore
    - server/spikes/pg_set_local.test.mpl
    - server/spikes/ws_actor_supervision.test.mpl
    - server/spikes/ws_reconnect_server.mpl
    - server/spikes/if_qualified_condition_and_string_aliases.test.mpl
key-decisions:
  - "Compose service renamed from app to server with build context anchored at ./server to match ownership boundaries."
  - "Root package.json remains dependency-light and only provides scoped wrapper scripts for server/client workflows."
patterns-established:
  - "Root-first commands: use dev:server/dev:client and build:server/build:client wrappers."
  - "Server-only spike assets belong in server/spikes while browser spikes stay in root spikes."
requirements-completed: []
duration: 5min
completed: 2026-03-03
---

# Phase 01.2 Plan 02: Compose Wiring and Scoped Root Commands Summary

**Compose now builds a `server` service from `server/`, root wrappers expose scoped server/client workflows, and server-only `.mpl` spikes moved to `server/spikes/`.**

## Performance

- **Duration:** 5 min
- **Started:** 2026-03-03T23:26:26Z
- **Completed:** 2026-03-03T23:31:26Z
- **Tasks:** 3
- **Files modified:** 9

## Accomplishments
- Moved backend Dockerfile ownership to `server/Dockerfile` and rewired Compose from `app` to `server`.
- Added root-scoped wrappers (`dev:*`, `build:*`, `test:*`, `migrate:*`) with zero root runtime dependencies.
- Cleanly separated server spike ownership by moving server `.mpl` spikes to `server/spikes/` while leaving browser `.html` spikes in root `spikes/`.

## Task Commits

Each task was committed atomically:

1. **Task 1: Move backend Dockerfile under server/ and rewire compose service** - `b0782c0` (refactor)
2. **Task 2: Add root-scoped command wrappers and update ignore paths** - `ff5deb2` (feat)
3. **Task 3: Move server-only spikes into server-owned location** - `4eb0a05` (refactor)

## Files Created/Modified
- `docker-compose.yml` - Renamed service `app` to `server` and set `build.context` to `./server`.
- `server/Dockerfile` - Backend Docker build definition moved from root into server ownership.
- `package.json` - Added root-scoped wrapper scripts for server/client build, dev, test, and migrations.
- `.gitignore` - Updated ignores from `frontend/*` to `client/*`.
- `server/spikes/*` - Moved server-owned `.mpl` spike assets into server-owned path.

## Decisions Made
- Aligned Compose service identity with directory boundary (`server`) rather than retaining legacy `app`.
- Kept root orchestration lightweight by using script wrappers only, with no root dependency hoisting.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Resolved transient git index lock during atomic task staging**
- **Found during:** Task 1 commit staging
- **Issue:** Parallel git staging produced `.git/index.lock` contention.
- **Fix:** Switched git staging/commit operations to sequential execution for task commits.
- **Files modified:** None (execution workflow only)
- **Verification:** Subsequent task commits completed cleanly.
- **Committed in:** `b0782c0` (task commit)

**2. [Rule 3 - Blocking] Manually updated STATE.md position/session after gsd-tools parse mismatch**
- **Found during:** Post-task state updates
- **Issue:** `state advance-plan` and `state record-session` could not parse the existing STATE.md structure.
- **Fix:** Updated current position and session continuity fields manually after successful task completion.
- **Files modified:** .planning/STATE.md
- **Verification:** STATE.md now reflects Plan 02 completion and next resume target.
- **Committed in:** Plan metadata commit

---

**Total deviations:** 2 auto-fixed (2 blocking)
**Impact on plan:** No scope change; deviations were execution reliability/state bookkeeping fixes.

## Issues Encountered
None

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- `01.2-03-PLAN.md` can proceed with docs terminology standardization and regression verification.
- No blockers from this plan.

---
*Phase: 01.2-reorganize-repo-with-server-and-client-directories*
*Completed: 2026-03-03*

## Self-Check: PASSED
