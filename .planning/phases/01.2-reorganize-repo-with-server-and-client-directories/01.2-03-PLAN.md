---
phase: 01.2-reorganize-repo-with-server-and-client-directories
plan: 03
type: execute
wave: 3
depends_on: ["01.2-02"]
files_modified:
  - README.md
  - CLAUDE.md
  - AGENTS.md
  - docker-compose.yml
  - package.json
  - client/src/lib/api.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Active docs and instructions consistently use `server/client` terminology."
    - "Root and direct build commands both validate after the reorg."
    - "Client-to-server API routing remains `/api` relative and behavior-compatible."
  artifacts:
    - path: "README.md"
      provides: "Current repo structure and root command workflow."
      contains: "server/, client/, dev:server, dev:client"
    - path: "CLAUDE.md"
      provides: "Updated local automation guidance with new paths/service names."
      contains: "server"
    - path: "AGENTS.md"
      provides: "Updated local agent guidance with new paths/service names."
      contains: "server"
  key_links:
    - from: "README.md"
      to: "package.json"
      via: "documented command wrappers"
      pattern: "dev:server|build:client"
    - from: "client/src/lib/api.ts"
      to: "/api"
      via: "relative API calls"
      pattern: "['\\\"]\\/api"
    - from: "docker-compose.yml"
      to: "server"
      via: "service naming contract"
      pattern: "^\\s{2}server:"
---

<objective>
Finish the reorg by standardizing active documentation and running a no-regression verification matrix.

Purpose: Ensure this structural phase is complete, understandable, and safe to execute downstream without hidden path drift.
Output: Updated docs plus confirmed build/runtime wiring across server, client, and compose workflows.
</objective>

<execution_context>
@/Users/sn0w/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.2-reorganize-repo-with-server-and-client-directories/01.2-CONTEXT.md
@.planning/phases/01.2-reorganize-repo-with-server-and-client-directories/01.2-RESEARCH.md
@CLAUDE.md
@AGENTS.md
@docker-compose.yml
@client/src/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardize active docs on server/client terminology and commands</name>
  <files>README.md, CLAUDE.md, AGENTS.md</files>
  <action>
Create a root `README.md` summarizing the new structure and root command workflow. Update `CLAUDE.md` and `AGENTS.md` so active instructions use `server/` and `client/` terminology, service name `server`, and scoped root commands from Plan 02. Do not rewrite historical phase records in `.planning/phases/**`; those remain immutable history.
  </action>
  <verify>
    <automated>rg -n "server/|client/|dev:server|dev:client|build:server|build:client" README.md CLAUDE.md AGENTS.md && ! rg -n "frontend/|services:\\s*app|\\bapp\\s+service" README.md CLAUDE.md AGENTS.md</automated>
  </verify>
  <done>Current contributor-facing docs are aligned with the reorg naming and command contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Run regression matrix and patch any missed path drift</name>
  <files>docker-compose.yml, package.json, client/src/lib/api.ts</files>
  <action>
Execute and confirm this matrix:
1. `meshc build server`
2. `npm --prefix client run build`
3. `npm run build:server`
4. `npm run build:client`
5. `docker compose config --services` includes `server`, `timescaledb`, `valkey` only
6. `client/src/lib/api.ts` still targets relative `/api`

If any check fails because of remaining path/name drift, patch only the impacted wiring file(s) and re-run until green.
  </action>
  <verify>
    <automated>meshc build server && npm --prefix client run build && npm run build:server && npm run build:client && SERVICES="$(docker compose config --services)" && echo "$SERVICES" | rg "^server$" && echo "$SERVICES" | rg "^timescaledb$" && echo "$SERVICES" | rg "^valkey$" && ! echo "$SERVICES" | rg "^app$" && rg -n "['\\\"]\\/api" client/src/lib/api.ts</automated>
  </verify>
  <done>All build and compose checks pass with `server/client` paths and no API behavior drift.</done>
</task>

</tasks>

<verification>
1. Docs and local instructions consistently use `server/client`.
2. Server build passes both direct and root-wrapper command paths.
3. Client build passes both direct and root-wrapper command paths.
4. Compose resolves only `server`, `timescaledb`, and `valkey`.
5. Client API adapter still uses relative `/api` routes.
</verification>

<success_criteria>
- Contributor docs are updated for the new repo layout.
- Reorg verification matrix is green end-to-end.
- No active path drift remains from `frontend/` or `app` service semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/01.2-reorganize-repo-with-server-and-client-directories/01.2-03-SUMMARY.md`
</output>
