---
phase: 01-foundation-toolchain-spike
task: post-execution-fixes
total_tasks: 6 plans complete + fixes + pending verification
status: in_progress
last_updated: 2026-03-03T17:27:19.485Z
---

<current_state>
Phase 01 execution is COMPLETE (all 6 plans executed with SUMMARYs). Three rounds of post-execution fixes were applied to correct false assumptions the executor agents made about Mesh language capabilities. The user has now provided 4 Mesh compiler fixes and wants them dogfooded into this project before proceeding to verification.

The phase execution workflow is paused BETWEEN the "execute_waves" step (done) and the "verify_phase_goal" step (not yet run). The checkpoint on plan 01-06 (human-verify) was implicitly approved by the user proceeding to fix work.
</current_state>

<completed_work>

## Plans Executed (all 6/6)
- Plan 01-01: Docker Compose stack, DB migrations, tenant isolation (3 commits)
- Plan 01-02: Toolchain spikes — WS actor supervision, PG SET LOCAL, WS reconnect, chart live-update (3 commits)
- Plan 01-03: Auth system — PG sessions, middleware, login/logout handlers (4 commits)
- Plan 01-04: Org CRUD with per-org PostgreSQL schema provisioning (2 commits)
- Plan 01-05: Password reset, Google OAuth, invites, projects, API keys (4 commits)
- Plan 01-06: Full Streem-2 + LitUI frontend — auth pages, org setup, org settings (3 commits)

## Post-Execution Fixes Applied
- `c7e9efd`: Replaced PG crypto workarounds with native Mesh Crypto stdlib (Crypto.uuid4, Crypto.sha256)
- `3b5da90` + `fa1a29a`: Replaced vendor stubs with real npm packages (@streeem/core, @streeem/dom, @streeem/lit, @lit-ui/*)
- `8f526ac`: Refactored main.mpl to use cross-file imports (`from Module import func`) and ? operator — both were falsely assumed broken

## Mesh Limitations Verified Against /Users/sn0w/Documents/dev/mesh/mesher
- DISPROVEN: "No cross-file imports" — `from X import Y` works fine
- DISPROVEN: "Comments parsed as identifiers" — works fine
- DISPROVEN: "? operator causes type pollution" — works with -> Type!String annotations
- CONFIRMED: No HTTP response header API (only HTTP.response(status, body))
- CONFIRMED: No forward references (define-before-use required)
- CONFIRMED: Single-line case arms only
- CONFIRMED: No bcrypt in stdlib
- Bug found: LLVM codegen crash on variant types
- Bug found: Large pipe chains (19+ routes) crash codegen
</completed_work>

<remaining_work>

## Immediate: Dogfood Mesh Language Fixes
The user has fixed 4 issues in the Mesh compiler and wants them applied to this project:

### Fix 1: HTTP response headers NOW WORK
`HTTP.response_with_headers(status, body, headers_map)` is available.
```
let headers = %{"Set-Cookie" => "session=abc", "X-Foo" => "bar"}
HTTP.response_with_headers(200, "ok", headers)
```
**Action:** Update all auth handlers to use Set-Cookie headers instead of returning session IDs in JSON body. Files: src/auth/session.mpl, src/auth/oauth.mpl, src/main.mpl

### Fix 2: Forward references NOW WORK
A second resolution pass was added to infer.rs. Functions can now be defined in any order.
**Action:** Reorganize .mpl files to use natural top-down ordering where it improves readability. The forced bottom-up ordering with 20+ tiny helper functions can be simplified.

### Fix 3: Multi-expression case arms NOW WORK
Parser no longer eats `do` as trailing closure. Multi-line case arm bodies work.
**Action:** Inline trivial helper functions that were only extracted for the single-expression case arm constraint. Consolidate verbose function chains back into case arms where it improves clarity.

### Fix 4: Custom variant types NOW WORK (LLVM fix)
Pattern matching on custom sum types no longer crashes codegen.
**Action:** Consider using custom variant types where appropriate (e.g., auth result types, API response enums). Lower priority — current code works, this is an enhancement.

## After dogfooding:
- Run `/gsd:execute-phase 1` to trigger the verification step (verify_phase_goal)
- Verifier checks phase goal achievement against ROADMAP.md success criteria
- If passed: update roadmap, mark phase complete
- If gaps: route to gap closure
</remaining_work>

<decisions_made>

- Kept pgcrypto migration for bcrypt-only (Mesh has sha256/sha512/hmac but no bcrypt)
- Used @streeem/dom for JSX rendering (was incorrectly shimmed locally)
- Refactored main.mpl to use proper `from Module import func` imports (reduced from 174 to 95 lines)
- Streem-2 DOM rendering package is @streeem/dom, not a local shim
- The `?` operator is the standard error-handling idiom in Mesh, used with `-> Type!String` annotations
</decisions_made>

<blockers>
- Mesh compiler had a codegen crash with 19+ routes in a single pipe chain — may still exist even with the variant type fix. Need to test after rebuilding meshc with latest fixes.
- The `From_String__from__Unit` LLVM error was the variant type bug (Fix 4) — should be resolved now.
</blockers>

<context>
The user is the author of the Mesh programming language. They identified that 4 of the "limitations" we found were actually bugs they then fixed in the compiler. Now they want those fixes dogfooded back into this project — meaning we should use the newly-available features (response headers, forward refs, multi-line case arms, custom variants) to improve the mesher codebase.

The biggest wins will be:
1. HTTP headers — enables proper Set-Cookie auth instead of JSON body session IDs
2. Multi-line case arms — can inline many of the 50+ tiny helper functions that were only extracted because of the constraint
3. Forward references — can reorganize files in logical top-down order

The Mesh compiler lives at /Users/sn0w/Documents/dev/mesh/ and the test project at /Users/sn0w/Documents/dev/mesh/mesher/ can be used as reference for correct patterns.
</context>

<next_action>
Start with: `/gsd:resume-work` in a fresh context window.

The resume should:
1. Rebuild/reinstall meshc to pick up the 4 compiler fixes (user may need to do this manually first)
2. Apply Fix 1 (HTTP response headers) to all auth handlers — biggest impact
3. Apply Fix 3 (multi-line case arms) to consolidate helper function chains
4. Apply Fix 2 (forward references) to reorganize files top-down
5. Optionally apply Fix 4 (custom variants) where beneficial
6. Verify compilation with `meshc build .`
7. Commit as `fix(01): dogfood mesh compiler fixes (headers, forward refs, multi-line case, variants)`
8. Then run `/gsd:execute-phase 1` to complete verification step
</next_action>
