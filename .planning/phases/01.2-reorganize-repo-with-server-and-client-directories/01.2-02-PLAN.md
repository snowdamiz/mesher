---
phase: 01.2-reorganize-repo-with-server-and-client-directories
plan: 02
type: execute
wave: 2
depends_on: ["01.2-01"]
files_modified:
  - server/Dockerfile
  - docker-compose.yml
  - package.json
  - .gitignore
  - server/spikes/
  - spikes/
autonomous: true
requirements: []

must_haves:
  truths:
    - "Root Docker Compose runs a `server` service that builds from `./server`."
    - "Root-first scoped commands exist for server and client workflows."
    - "Dependency ownership remains app-scoped (no hoisted root runtime dependencies)."
    - "Server-only spike artifacts are owned under `server/spikes/`."
  artifacts:
    - path: "docker-compose.yml"
      provides: "Service rename `app`->`server` and server build context wiring."
      contains: "services.server.build"
    - path: "server/Dockerfile"
      provides: "Backend Docker build definition in server-owned location."
      contains: "COPY mesh.toml"
    - path: "package.json"
      provides: "Root command entrypoints (`dev:server`, `dev:client`, `build:*`)."
      contains: "scripts"
    - path: ".gitignore"
      provides: "Client path ignores after rename."
      contains: "client/node_modules"
    - path: "server/spikes/"
      provides: "Server-owned `.mpl` spike assets moved out of root ownership."
      contains: ".test.mpl"
  key_links:
    - from: "docker-compose.yml"
      to: "server/Dockerfile"
      via: "build context + dockerfile path"
      pattern: "build:"
    - from: "package.json"
      to: "server/"
      via: "meshc command targets"
      pattern: "meshc .* server"
    - from: "package.json"
      to: "client/"
      via: "npm --prefix client wrappers"
      pattern: "npm --prefix client"
    - from: "server/spikes/ws_reconnect_server.mpl"
      to: "spikes/ws_reconnect.test.html"
      via: "server counterpart for browser spike"
      pattern: "ws_reconnect"
---

<objective>
Rewire root orchestration and command entrypoints to match the new `server/` + `client/` layout, then finalize spike ownership boundaries.

Purpose: Preserve the locked root-first developer workflow while aligning Compose and Docker ownership with the directory split.
Output: Updated Compose service semantics, backend Dockerfile placement, scoped root commands, and server-only spike relocation.
</objective>

<execution_context>
@/Users/sn0w/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.2-reorganize-repo-with-server-and-client-directories/01.2-CONTEXT.md
@.planning/phases/01.2-reorganize-repo-with-server-and-client-directories/01.2-RESEARCH.md
@docker-compose.yml
@Dockerfile
@.gitignore
@spikes/ws_actor_supervision.test.mpl
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move backend Dockerfile under server/ and rewire compose service</name>
  <files>server/Dockerfile, docker-compose.yml</files>
  <action>
Move root `Dockerfile` to `server/Dockerfile` (`git mv`). Update `docker-compose.yml` by renaming service `app` to `server`, pointing build context to `./server` with Dockerfile in that directory, and preserving existing port, env vars, dependency ordering, and restart behavior. Keep runtime behavior unchanged; this is a structural wiring update only.
  </action>
  <verify>
    <automated>SERVICES="$(docker compose config --services)" && echo "$SERVICES" | rg "^server$" && ! echo "$SERVICES" | rg "^app$" && docker compose config | rg "context: .*/server$"</automated>
  </verify>
  <done>Compose resolves a `server` service (not `app`) and builds it from the `server/` directory.</done>
</task>

<task type="auto">
  <name>Task 2: Add root-scoped command wrappers and update ignore paths</name>
  <files>package.json, .gitignore</files>
  <action>
Create a root `package.json` (private workspace wrapper only, no runtime dependencies) with scoped scripts:
- `dev:server`, `build:server`, `test:server`, `migrate:status`, `migrate:up`
- `dev:client`, `build:client`, `test:client`

Server scripts must call Mesh commands against `server` explicitly (for example `meshc build server`). Client scripts must delegate with `npm --prefix client ...`. Update `.gitignore` from `frontend/*` ignores to `client/*` equivalents.
  </action>
  <verify>
    <automated>node -e 'const p=require("./package.json"); const need=["dev:server","dev:client","build:server","build:client","migrate:status","migrate:up"]; for (const k of need){ if(!p.scripts || !p.scripts[k]) throw new Error("missing script "+k); } if (p.dependencies && Object.keys(p.dependencies).length) throw new Error("root dependencies must stay empty");' && rg -n "client/node_modules|client/dist" .gitignore</automated>
  </verify>
  <done>Root-first command workflow exists with scoped naming and app-scoped dependency boundaries remain intact.</done>
</task>

<task type="auto">
  <name>Task 3: Move server-only spikes into server-owned location</name>
  <files>server/spikes/, spikes/</files>
  <action>
Create `server/spikes/` and move server-side `.mpl` spikes there: `pg_set_local.test.mpl`, `ws_actor_supervision.test.mpl`, `ws_reconnect_server.mpl`, `mesh_language_issues_v3.test.mpl`, and `if_qualified_condition_and_string_aliases.test.mpl`. Keep browser `.html` spikes under root `spikes/`; this satisfies the locked decision to remove server-only spike ownership from root.
  </action>
  <verify>
    <automated>test -d server/spikes && test -f server/spikes/pg_set_local.test.mpl && test -f server/spikes/ws_actor_supervision.test.mpl && test -f server/spikes/ws_reconnect_server.mpl && test -f server/spikes/mesh_language_issues_v3.test.mpl && test -f server/spikes/if_qualified_condition_and_string_aliases.test.mpl && test -f spikes/chart_live_update.test.html && test -f spikes/ws_reconnect.test.html</automated>
  </verify>
  <done>Server spike ownership is cleanly separated into `server/spikes/` with browser spikes still available.</done>
</task>

</tasks>

<verification>
1. `docker compose config --services` includes `server`, `timescaledb`, and `valkey`.
2. Compose output does not contain service `app`.
3. Root `package.json` exposes scoped server/client command entrypoints.
4. `.gitignore` aligns with `client/` paths.
5. Server-only `.mpl` spike files are moved to `server/spikes/` and browser `.html` spikes remain in root `spikes/`.
</verification>

<success_criteria>
- Backend Dockerfile ownership moves under `server/`.
- Compose uses `server` service name and `./server` build context.
- Root workflow supports `dev:server`, `dev:client`, `build:server`, and `build:client`.
- Root stays dependency-light; client and server keep app-scoped ownership.
- `server/spikes/` contains moved `.mpl` spikes while root `spikes/` retains browser `.html` spikes.
</success_criteria>

<output>
After completion, create `.planning/phases/01.2-reorganize-repo-with-server-and-client-directories/01.2-02-SUMMARY.md`
</output>
